name: CWS Advanced Stats Analysis

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape-and-analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create directories
        run: mkdir -p data results

      - name: Scrape NCAA team stats
        run: python scrape_ncaa_stats.py
        env:
          PYTHONUNBUFFERED: "1"

      - name: Run analysis
        run: python analyze_cws_stats.py

      - name: Generate visualizations
        run: python visualize_results.py

      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ results/ 2>/dev/null || true
          git diff --cached --quiet || git commit -m "Update CWS analysis $(date +%Y-%m-%d)"
          git push || echo "Nothing to push"
```

---

### `requirements.txt`
```
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0
scikit-learn>=1.1.0
matplotlib>=3.6.0
seaborn>=0.12.0
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
tqdm>=4.64.0
ncaa_bbStats
